'use server'
interface props {
    name: string;
    email: string;
    password: string;
    passwordConfirm: string;
}

export const register = ({name, email, password, passwordConfirm}: props) =>{

}